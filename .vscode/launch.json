{
    "version": "0.0.1",
    "configurations": [
        {
           "name": "DEBUG",
            "type": "cppdbg", "request": "launch", "MIMode": "lldb",
            "stopAtEntry": false,
            "miDebuggerPath": "/usr/bin/lldb",
            "preLaunchTask" : "make debug",
            "program": "${workspaceFolder}/test",
            "cwd": "${workspaceFolder}"
        }
    ]
}